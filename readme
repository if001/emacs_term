
M-x byte-compile
.elファイルをバイトコンパイルする
コンパイルすることで.elcファイルが生成される
設定ファイルinit.elでrequireにより.el(.elc)ファイルを呼び出すことで
elファイルが使えるようになる。
.elcファイルは.elファイルに比べ読み込みが早い

auto-install
パッケージをインストールする方法の一つ
M-x install-elisp RET
で利用可能
URLを求められるので、アドレスを入力してダウンロード開始
完了後C-c C-cでバイトコンパイル、パスの通ったファイルへの配置を自動で行ってくれる。
auto-installによりインストールしたものは.emacs以下に配置される

melpa：パッケージマネージャー
拡張機能のインストールに便利
melpaなどでダウンロードしてきたパッケージは
/elpaディレクトリに保存されるので、/elisp(パスの通ったファイル)に配置する必要がある。

anything:候補選択型インターフェイス
 




